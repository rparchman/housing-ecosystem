version: "3.9"

services:
  scheduler:
    build: ./scheduler
    container_name: gis_scheduler
    volumes:
      - ./pipeline:/app/pipeline
      - ./pipeline/config:/app/pipeline/config
    restart: always
